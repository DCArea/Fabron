using Orleans.TestKit;

namespace TGH.Test.Grains.CronJob
{

    public class CronJobGrainTests : TestKitBase
    {
        // [Fact]
        // public async Task Create()
        // {
        //     var cronJobId = Guid.NewGuid();
        //     var grain = await Silo.CreateGrainAsync<CronJobGrain>(cronJobId);
        //     // var cronExp = Cronos.CronExpression.

        //     // await grain.Create(roles, players);

        //     var state = await grain.GetState();
        // }
    }
}

